version: '3.8'

services:
  web-resume:
    image: nginx:alpine
    container_name: web-resume
    ports:
      - "8082:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped